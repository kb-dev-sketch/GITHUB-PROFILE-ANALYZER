# Frontend

## Run

1. Start the API from the project root (use port **3001**, not 5000 — macOS AirPlay uses 5000):

```bash
# In project root .env
PORT=3001

npm run dev
```

2. Start the frontend:

```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:5173
